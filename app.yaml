runtime: nodejs
env: flex

runtime_config:
  operating_system: 'ubuntu22'
  runtime_version: '20'

manual_scaling:
  instances: 1

includes:
  - env.yaml

beta_settings:
  cloud_sql_instances: euphoric-world-440804-k1:asia-southeast1:db-pg-bejskm7
